{
  "name": "wrist-mcp",
  "version": "0.1.0",
  "description": "MCP server that lets Claude and other agents search your Wrist captures (read from your Obsidian vault).",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "bin": {
    "wrist-mcp": "dist/src/index.js"
  },
  "main": "dist/src/index.js",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "tsc && node --test dist/test/*.test.js",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
